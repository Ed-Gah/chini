import {Text} from 'native-base';
import React from 'react';
import {Colors} from '../constants/Colors';

export default function Logo() {
  return (
    <Text fontSize={'5xl'} color={Colors.primary}>
      LOGO
    </Text>
  );
}
